import Rae from 'rae';
import Combinatorics from 'js-combinatorics';
import utils from './utils/index.ts';

const letters = ['a', 'c', 'e', 'i', 't', 'r', 'n', 'u', 'o'];

const bestWord = utils.getBestWord(letters);
